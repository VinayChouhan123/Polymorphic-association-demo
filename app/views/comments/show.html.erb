<div>
	Title : <%= @comment.title %><br>
</div>
<%= link_to "Edit Comment", edit_comment_path(@comment) %> | 
<%= link_to "Delete Comment", comment_path(@comment), data: { method: :delete, confirm: "Are you sure?" } %>
